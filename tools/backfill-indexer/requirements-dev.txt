# Test/dev dependencies for the backfill Indexer.
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.0
numpy>=1.24      # deterministic test-image generation + fast fallback grayscale
piexif>=1.1.3    # writes known EXIF blocks into test JPEGs
